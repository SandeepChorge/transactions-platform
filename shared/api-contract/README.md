# api-contract

OpenAPI spec shared by `transactions-parser-web` and `transactions-parser-android`. Both clients
generate their API models/clients from the spec here, so the contract has a single source of
truth instead of drifting per-client.

Not yet populated — add `openapi.yaml` once `services/transactions-api` has its first endpoints.
