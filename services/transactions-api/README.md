# transactions-api

Not yet scaffolded. Backend API for the transactions platform. Its OpenAPI spec will live in
`shared/api-contract` so both `transactions-parser-web` and `transactions-parser-android` can
generate clients from it.
